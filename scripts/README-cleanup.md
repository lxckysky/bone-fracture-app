# 🧹 Database Cleanup Script - คู่มือการใช้งาน

สคริปต์สำหรับลบข้อมูลรูป X-ray และประวัติการวิเคราะห์เก่าๆ ออกจากระบบ

## 📋 วิธีใช้งาน

### 1️⃣ ลบข้อมูลทั้งหมด
```bash
npm run cleanup -- all
```
⚠️ **คำเตือน**: จะลบข้อมูลทั้งหมดในระบบ! (มีเวลายืนยัน 5 วินาที)

### 2️⃣ ลบข้อมูลเก่ากว่า X วัน
```bash
npm run cleanup -- older_than_days 30
```
ตัวอย่าง: ลบข้อมูลที่เก่ากว่า 30 วัน

### 3️⃣ ลบข้อมูลของ User คนใดคนหนึ่ง
```bash
npm run cleanup -- by_user <user_id>
```
ตัวอย่าง: `npm run cleanup -- by_user 123e4567-e89b-12d3-a456-426614174000`

## 🎯 การทำงาน

สคริปต์จะทำงาน 2 ขั้นตอน:
1. **ลบไฟล์รูป** จาก Supabase Storage (bucket: `scans`)
2. **ลบข้อมูล** จากตาราง `cases` ในฐานข้อมูล

## 📊 ตัวอย่างผลลัพธ์

```
🧹 เริ่มทำความสะอาดข้อมูล...
📅 จะลบข้อมูลที่เก่ากว่า 30 วัน (ก่อน 15/01/2026)
📊 พบข้อมูล 45 รายการที่ต้องลบ
🗑️  กำลังลบไฟล์รูป 45 ไฟล์...
✅ ลบไฟล์รูปสำเร็จ 45 ไฟล์
🗃️  กำลังลบข้อมูลใน Database...
✅ ลบข้อมูลสำเร็จ 45 รายการ
🎉 ทำความสะอาดเสร็จสิ้น!
```

## ⚠️ ข้อควรระวัง

- ข้อมูลที่ลบแล้ว **ไม่สามารถกู้คืนได้**
- ควรทำ backup ข้อมูลก่อนใช้คำสั่ง `all`
- ตรวจสอบให้แน่ใจว่า `.env.local` มี Supabase credentials ถูกต้อง

## 🔧 ไฟล์ที่เกี่ยวข้อง

- สคริปต์: [`scripts/cleanup-database.ts`](file:///c:/Users/hp/Desktop/Bone%20demo/scripts/cleanup-database.ts)
- Package config: [`package.json`](file:///c:/Users/hp/Desktop/Bone%20demo/package.json)
