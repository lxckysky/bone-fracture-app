# AI Model Directory

Please place your YOLO11x-cls model file here with the name `model.pt`.

Path: `backend/models/model.pt`

If the model is not found, the system will run in **Mock/Simulation Mode**, returning random predictions for testing purposes.
